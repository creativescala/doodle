package doodle
package chart
package syntax

/*
import scala.language.higherKinds

trait ToCategoricalSyntax {
  implicit class ToCategoricalOps[F[_],A](a: F[A]) {
    def toCategorical(implicit t: ToCategorical[F,A]): Categorical[F,A] =
      t.toCategorical(a)
  }
}

trait ToRatioSyntax {
  implicit class ToRatioOps[F[_],A](a: F[A]) {
    def toRatio(implicit p: ToRatio[F,A]): Ratio[F] =
      p.toRatio(a)
  }
}
 */
